// examples/demo.ltl — runnable demo for ltl_shamir

import std::io
use ltl_shamir as lib

fn main() {
    io::println("ltl_shamir demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}